% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcano.R
\name{plot_regression_volcano}
\alias{plot_regression_volcano}
\title{Plot a volcano-style regression summary}
\usage{
plot_regression_volcano(
  data,
  effect_col = NULL,
  p_col = "pvalue",
  adjusted_p_col = NULL,
  label_col = NULL,
  significance_cutoff = 0.05,
  top_n_label_each = 5,
  null_effect = 1,
  x_lab = NULL,
  y_lab = NULL,
  x_limits = NULL,
  y_limits = NULL,
  point_size = 1.05,
  label_size = 2,
  colors = c(neutral = "#D8D8D8", lower = "#2F6FA3", higher = "#C74732"),
  show_cutoff = TRUE
)
}
\arguments{
\item{data}{A data.frame containing regression results.}

\item{effect_col}{Character. Column containing the effect estimate to plot on
the x-axis. If \code{NULL}, the function uses \code{HR}, then \code{OR}, then \code{estimate}
when available.}

\item{p_col}{Character. Column containing raw p-values. Default \code{"pvalue"}.}

\item{adjusted_p_col}{Optional character. Column used for highlighting
significant points, such as \code{"p_bonferroni"} or \code{"p_bh"}. If \code{NULL},
\code{p_col} is used.}

\item{label_col}{Optional character. Column used for point labels. If \code{NULL},
the function uses \code{gene_symbol}, then \code{protein_clean}, then \code{variable} when
available.}

\item{significance_cutoff}{Numeric cutoff applied to \code{adjusted_p_col}.
Default \code{0.05}.}

\item{top_n_label_each}{Integer. Number of highlighted proteins to label from
each direction. Direction is defined relative to \code{null_effect}.}

\item{null_effect}{Numeric null effect. Use \code{1} for ratio estimates such as
HR/OR and \code{0} for beta estimates. Default \code{1}.}

\item{x_lab, y_lab}{Axis labels. If \code{NULL}, defaults are generated.}

\item{x_limits, y_limits}{Optional numeric vectors of length 2 for axis
limits.}

\item{point_size}{Numeric point size. Default \code{1.05}.}

\item{label_size}{Numeric label size. Default \code{2}.}

\item{colors}{Named character vector for groups \code{neutral}, \code{lower}, and
\code{higher}.}

\item{show_cutoff}{Logical. Whether to draw a horizontal significance cutoff
line. Default \code{TRUE}.}
}
\value{
A \code{ggplot2} object with attributes \code{plot_data} and \code{label_data}.
}
\description{
Create a volcano-style plot from regression summary results such as
\code{\link[=runmulti_cox]{runmulti_cox()}} or \code{\link[=runmulti_logit]{runmulti_logit()}}. The x-axis shows the supplied effect
estimate column (for example \code{HR} or \code{OR}), and the y-axis shows
\code{-log10(P)}. Points can be highlighted by an adjusted p-value column, while
labels are selected from the largest and smallest highlighted effects.
}
\examples{
\dontrun{
cox_res$p_bonferroni <- p.adjust(cox_res$pvalue, method = "bonferroni")
plot_regression_volcano(
  cox_res,
  effect_col = "HR",
  p_col = "pvalue",
  adjusted_p_col = "p_bonferroni",
  label_col = "gene_symbol"
)
}

}
