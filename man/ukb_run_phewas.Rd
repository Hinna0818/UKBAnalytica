% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas_phewas_tools.R
\name{ukb_run_phewas}
\alias{ukb_run_phewas}
\title{Run PheWAS Using the PheWAS R Package}
\usage{
ukb_run_phewas(
  x,
  genotypes,
  genotype_id_col = "IID",
  genotype_cols = NULL,
  covariate_cols = x$settings$covariates,
  min_code_count = 1L,
  min_records = 20L,
  cores = 1L,
  additive_genotypes = TRUE,
  add_phecode_exclusions = TRUE,
  sex_restrictions = TRUE,
  vocabulary_map = c("icd10", "default"),
  significance_threshold = c("p-value", "bonferroni", "fdr"),
  alpha = 0.05
)
}
\arguments{
\item{x}{A \code{ukb_gwas_phewas_phenotype}.}

\item{genotypes}{Data frame containing participant IDs and one or more
additive genotype/dosage columns.}

\item{genotype_id_col}{Participant ID column in \code{genotypes}.}

\item{genotype_cols}{Genotype columns. Defaults to every non-ID column.}

\item{covariate_cols}{Covariates from \code{x} used in every model.}

\item{min_code_count}{Minimum code count used by
\code{PheWAS::createPhenotypes}.}

\item{min_records}{Minimum case/control records used by
\code{PheWAS::phewas}.}

\item{cores}{Number of R worker processes.}

\item{additive_genotypes}{Treat genotype inputs as additive dosages.}

\item{add_phecode_exclusions}{Apply phecode control exclusions.}

\item{sex_restrictions}{Apply sex-specific phenotype restrictions when a
sex table was prepared in \code{x}.}

\item{vocabulary_map}{PheWAS vocabulary map: \code{"icd10"} matches the
official RAP tutorial, while \code{"default"} supports the package's
combined map.}

\item{significance_threshold}{Threshold columns requested from PheWAS.}

\item{alpha}{Family-wise/FDR alpha.}
}
\value{
A list of class \code{ukb_phewas_result}.
}
\description{
Convert the long ICD table in a
\code{ukb_gwas_phewas_phenotype} object to phecodes and run the external
PheWAS R package. The dependency is optional and is never installed
automatically.
}
