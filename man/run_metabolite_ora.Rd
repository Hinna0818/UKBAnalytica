% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Metabolomics.R
\name{run_metabolite_ora}
\alias{run_metabolite_ora}
\title{Run metabolite over-representation analysis}
\usage{
run_metabolite_ora(
  metabolites,
  pathway_db = NULL,
  universe = NULL,
  backend = c("custom", "metaboanalyst"),
  id_type = "name",
  library = "smpdb_pathway",
  mapping_table = NULL,
  pathway_col = "pathway",
  metabolite_col = "metabolite",
  min_metabolites = 3,
  p_adjust_method = "BH",
  run_subprocess = TRUE
)
}
\arguments{
\item{metabolites}{Character vector of metabolite names.}

\item{pathway_db}{Optional data.frame for custom ORA. Must contain pathway
and metabolite columns.}

\item{universe}{Optional background metabolite vector. If \code{NULL}, the
pathway library metabolites are used for custom ORA.}

\item{backend}{One of \code{"custom"} or \code{"metaboanalyst"}.}

\item{id_type}{Metabolite identifier type for MetaboAnalystR
cross-referencing. Default \code{"name"}.}

\item{library}{MetaboAnalystR metabolite-set library. Default
\code{"smpdb_pathway"}.}

\item{mapping_table}{Optional custom mapping table passed to
\code{metabolite_to_metaboanalyst_name()}.}

\item{pathway_col}{Column name in \code{pathway_db} containing pathway names.
Default \code{"pathway"}.}

\item{metabolite_col}{Column name in \code{pathway_db} containing metabolite
names. Default \code{"metabolite"}.}

\item{min_metabolites}{Minimum mapped metabolites required for ORA.
Default \code{3}.}

\item{p_adjust_method}{Multiple-testing method used by \code{\link[=p.adjust]{p.adjust()}}.
Default \code{"BH"}.}

\item{run_subprocess}{Logical. For \code{backend = "metaboanalyst"}, run
MetaboAnalystR in a clean subprocess to avoid global-state issues. Default
\code{TRUE}.}
}
\value{
A list of class \code{ukb_metabolite_ora} with components \code{input},
\code{mapping}, \code{matched}, \code{unmatched}, \code{ora_result}, \code{backend}, \code{library}, and
\code{mapping_summary}.
}
\description{
Run ORA for a metabolite list. The recommended first backend is
\code{backend = "custom"}, where users provide a two-column metabolite pathway
library. A \code{backend = "metaboanalyst"} interface is also provided for users
who have installed MetaboAnalystR and want to use its metabolite-set
libraries, such as \code{"smpdb_pathway"}.
}
\examples{
panel <- load_ukb_metabolite_panel()
hits <- c("Alanine", "Glutamine", "Glycine", "Lactate", "Pyruvate")
pathway_db <- data.frame(
  pathway = c(rep("Amino acid metabolism", 3), rep("Energy metabolism", 2)),
  metabolite = c("L-Alanine", "L-Glutamine", "Glycine", "Lactic acid", "Pyruvic acid")
)
run_metabolite_ora(hits, pathway_db = pathway_db, backend = "custom")

}
