% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gp_primary_care.R
\name{rap_plan_gp_query}
\alias{rap_plan_gp_query}
\title{Plan a RAP Spark SQL GP Query}
\usage{
rap_plan_gp_query(
  query,
  database = NULL,
  table = "gp_clinical",
  cohort_table = NULL,
  cohort_database = database,
  collect = c("records", "summary")
)
}
\arguments{
\item{query}{A \code{ukb_gp_query} from \code{\link[=parse_gp_query]{parse_gp_query()}}.}

\item{database}{Optional RAP Spark database name. If \code{NULL}, the active
database of the supplied Spark connection is used.}

\item{table}{GP clinical table name. Defaults to the stable
\code{"gp_clinical"} view.}

\item{cohort_table}{Optional Spark table/view containing an \code{eid} column.
When supplied, a left-semi join restricts GP records before collection.}

\item{cohort_database}{Optional database containing \code{cohort_table}. Defaults
to \code{database}.}

\item{collect}{Return record-level SQL (\code{"records"}) or push participant by
disease aggregation into Spark (\code{"summary"}).}
}
\value{
A \code{ukb_gp_query_plan} containing the SQL and normalized query.
}
\description{
Creates auditable Spark SQL that selects only requested Read v2 / CTV3
records from the stable RAP \code{gp_clinical} view. It does not execute the
query or load GP records into R.
}
