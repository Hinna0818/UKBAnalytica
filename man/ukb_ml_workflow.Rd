% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_workflow.R
\name{ukb_ml_workflow}
\alias{ukb_ml_workflow}
\title{Run a Frozen-Test UKB ML Workflow}
\usage{
ukb_ml_workflow(
  formula,
  data = NULL,
  split = NULL,
  model,
  outcome_type = c("auto", "binary", "multiclass", "continuous"),
  split_params = list(),
  feature_select = c("none", "boruta", "filter", "glmnet"),
  feature_params = list(),
  tune = TRUE,
  tune_params = list(),
  threshold_method = c("none", "fixed", "youden"),
  threshold_params = list(),
  fit_final = TRUE,
  evaluate_test = TRUE,
  seed = NULL,
  verbose = TRUE,
  ...,
  nested_cv = FALSE,
  nested_cv_params = list()
)
}
\arguments{
\item{formula}{Model formula.}

\item{data}{Optional full dataset. Required when \code{split} is NULL.}

\item{split}{Optional \code{ukb_ml_split} object.}

\item{model}{Model type: \code{"logistic"}, \code{"linear"}, \code{"rf"},
\code{"xgboost"}, \code{"glmnet"}, \code{"svm"}, \code{"nnet"},
\code{"rpart"}, or \code{"naive_bayes"}.}

\item{outcome_type}{\code{"auto"}, \code{"binary"}, \code{"multiclass"}, or
\code{"continuous"}.}

\item{split_params}{List passed to \code{\link{ukb_ml_split_data}} when
\code{split} is NULL.}

\item{feature_select}{\code{"none"}, \code{"boruta"}, \code{"filter"}, or
\code{"glmnet"}.}

\item{feature_params}{List passed to \code{\link{ukb_ml_feature_select}}.}

\item{tune}{Logical. Run hyperparameter tuning.}

\item{tune_params}{List passed to \code{\link{ukb_ml_tune}}.}

\item{threshold_method}{\code{"none"}, \code{"fixed"}, or \code{"youden"}.}

\item{threshold_params}{List passed to \code{\link{ukb_ml_threshold}}.}

\item{fit_final}{Logical. Refit final model.}

\item{evaluate_test}{Logical. Evaluate once on frozen test set.}

\item{seed}{Optional random seed.}

\item{verbose}{Logical.}

\item{...}{Additional arguments.}

\item{nested_cv}{Logical. Run supplementary nested cross-validation on the
training split. Defaults to \code{FALSE} and does not alter the standard
workflow when disabled.}

\item{nested_cv_params}{Optional list containing \code{outer_folds},
\code{inner_folds}, \code{repeats}, \code{metric}, \code{seed}, or
\code{verbose} for \code{\link{ukb_ml_nested_cv}}.}
}
\value{
A \code{ukb_ml_workflow} object containing the frozen data split,
optional \code{nested_cv_result}, feature-selection and tuning results,
threshold information, the refitted final model, and final test metrics
and predictions.
}
\description{
High-level, publication-oriented ML workflow for binary, multiclass, and
continuous outcomes. The test set is frozen before feature selection,
hyperparameter tuning, threshold learning, and final refit.
}
