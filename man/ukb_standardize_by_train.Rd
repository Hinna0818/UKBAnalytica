% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cox_workflow.R
\name{ukb_standardize_by_train}
\alias{ukb_standardize_by_train}
\title{Standardize variables using training-set parameters}
\usage{
ukb_standardize_by_train(
  train_data,
  validation_data = NULL,
  variables,
  center = TRUE,
  scale = TRUE
)
}
\arguments{
\item{train_data}{Training data.}

\item{validation_data}{Optional validation data.}

\item{variables}{Character vector of variables to standardize.}

\item{center}{Logical. If TRUE, subtract the training-set mean.}

\item{scale}{Logical. If TRUE, divide by the training-set standard deviation.}
}
\value{
A list with \code{train}, \code{validation}, and \code{parameters}.
}
\description{
Standardize a set of variables in the training data and optionally apply the
same centering and scaling parameters to a validation data set. This is useful
for omics analyses where all downstream association estimates should be
expressed per one training-set standard deviation.
}
\examples{
dat <- data.frame(x = 1:5, y = c(2, 3, 5, 7, 11))
ukb_standardize_by_train(dat, variables = c("x", "y"))$parameters
}
