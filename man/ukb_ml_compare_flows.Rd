% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_workflow.R
\name{ukb_ml_compare_flows}
\alias{ukb_ml_compare_flows}
\title{Compare Multiple Feature Sets and/or Models}
\usage{
ukb_ml_compare_flows(
  formula = NULL,
  data = NULL,
  split = NULL,
  train_data = NULL,
  test_data = NULL,
  validation_data = NULL,
  id_col = NULL,
  outcome = NULL,
  feature_sets = NULL,
  features = NULL,
  models = "xgboost",
  compare = c("auto", "feature_sets", "models", "both"),
  outcome_type = c("auto", "binary", "multiclass", "continuous"),
  feature_set_labels = NULL,
  model_labels = NULL,
  param_grid = NULL,
  tune_params = list(),
  threshold_params = list(),
  ...
)
}
\arguments{
\item{formula}{Optional base formula. The response is used as the outcome.
Predictors are used as the default feature set when \code{feature_sets} is
\code{NULL}.}

\item{data, split, train_data, test_data, validation_data, id_col}{Passed to
\code{\link{ukb_ml_flow}}.}

\item{outcome}{Optional outcome column. Required when \code{formula} is
\code{NULL}.}

\item{feature_sets}{Optional named list of feature vectors. If \code{NULL},
one feature set is derived from \code{formula} or \code{features}.}

\item{features}{Optional feature names used when \code{formula} and
\code{feature_sets} are \code{NULL}.}

\item{models}{Character vector of models supported by
\code{\link{ukb_ml_supported_models}}.}

\item{compare}{Comparison mode: \code{"auto"}, \code{"feature_sets"},
\code{"models"}, or \code{"both"}. In \code{"auto"} mode, all supplied
feature-set and model combinations are evaluated.}

\item{outcome_type}{Outcome type passed to \code{\link{ukb_ml_flow}}.}

\item{feature_set_labels}{Optional labels for feature sets.}

\item{model_labels}{Optional labels for models.}

\item{param_grid}{Optional hyperparameter grid. Can be a single grid shared
by all combinations, a named list keyed by model, feature set, or
\code{"feature_set__model"}.}

\item{tune_params}{Optional list passed to \code{\link{ukb_ml_tune}}. Can also
be keyed by model, feature set, or combination.}

\item{threshold_params}{Optional list passed to \code{\link{ukb_ml_threshold}}.
Can also be keyed by model, feature set, or combination.}

\item{...}{Additional arguments passed to \code{\link{ukb_ml_flow}}, including
\code{outcome_type}, \code{split_params}, \code{threshold_method},
\code{metrics}, \code{positive_class}, \code{use_validation_in_refit},
\code{compute_shap}, \code{shap_params}, \code{seed}, and \code{verbose}.}
}
\value{
A \code{ukb_ml_flow_compare} object containing \code{flows},
\code{metrics}, \code{comparison}, \code{predictions}, \code{roc}, and
\code{thresholds}.
}
\description{
Batch-runs \code{\link{ukb_ml_flow}} across feature-set and model
combinations. The same frozen train/test split is reused for every
combination, making the output suitable for comparing different feature
groups, different machine-learning algorithms, or the full
feature-set-by-model grid.
}
\examples{
\dontrun{
cmp <- ukb_ml_compare_flows(
  split = split,
  outcome = "copd",
  feature_sets = list(
    clinical = c("age", "sex", "bmi"),
    combined = c("age", "sex", "bmi", protein_vars)
  ),
  models = c("logistic", "xgboost"),
  compare = "both"
)
plot(cmp)
}

}
