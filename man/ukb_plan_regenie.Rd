% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas_phewas_tools.R
\name{ukb_plan_regenie}
\alias{ukb_plan_regenie}
\title{Plan a REGENIE GWAS}
\usage{
ukb_plan_regenie(
  files,
  bed_prefix,
  step1_extract,
  step2_bgen,
  step2_sample,
  output_prefix,
  phenotypes = NULL,
  covariate_file = NULL,
  covariates = NULL,
  categorical_covariates = NULL,
  trait_type = NULL,
  keep_file = NULL,
  step2_extract = NULL,
  prediction_file = NULL,
  ref_first = TRUE,
  firth = TRUE,
  spa = FALSE,
  p_threshold = 0.01,
  bsize_step1 = 1000L,
  bsize_step2 = 400L,
  threads = NULL,
  lowmem = TRUE,
  step1_args = character(),
  step2_args = character()
)
}
\arguments{
\item{files}{A \code{ukb_gwas_phewas_files} object or path to a
REGENIE-compatible phenotype file.}

\item{bed_prefix}{Step 1 PLINK BED/BIM/FAM prefix, without extension.}

\item{step1_extract}{Step 1 variant inclusion file.}

\item{step2_bgen}{One or more Step 2 BGEN paths.}

\item{step2_sample}{One sample path or one path per BGEN.}

\item{output_prefix}{Output prefix in RAP-controlled storage.}

\item{phenotypes}{Phenotype columns. Automatically obtained when
\code{files} is a UKBAnalytica files object.}

\item{covariate_file}{Optional covariate file. Defaults to the generated
REGENIE-compatible covariate file.}

\item{covariates}{Covariate columns.}

\item{categorical_covariates}{Subset of covariates treated as categorical.}

\item{trait_type}{\code{"binary"} or \code{"quantitative"}. Automatically
obtained when all selected traits have the same type.}

\item{keep_file}{Optional two-column FID/IID keep file. Defaults to the
generated sample file.}

\item{step2_extract}{Optional Step 2 variant inclusion file. Supply one path
or one path per BGEN.}

\item{prediction_file}{Step 1 prediction-list path expected by Step 2. If
\code{NULL}, uses REGENIE's conventional
\code{<output_prefix>_step1_pred.list} path.}

\item{ref_first}{Add \code{--ref-first} to Step 2.}

\item{firth}{Use Firth fallback for binary traits.}

\item{spa}{Use SPA instead of Firth fallback for binary traits. Only one of
\code{firth} and \code{spa} may be true.}

\item{p_threshold}{P-value threshold for approximate Firth/SPA fallback.}

\item{bsize_step1, bsize_step2}{REGENIE block sizes.}

\item{threads}{Optional positive integer thread count.}

\item{lowmem}{Use REGENIE Step 1 low-memory mode.}

\item{step1_args, step2_args}{Additional REGENIE argument tokens appended to
the corresponding step. Flags managed by this wrapper are rejected; use
\code{\link{ukb_plan_regenie_command}} for a fully custom command.}
}
\value{
A list of class \code{ukb_regenie_plan}.
}
\description{
Build auditable REGENIE Step 1 and Step 2 command arguments from files
produced by \code{\link{ukb_write_gwas_phewas_phenotype}}. This function
does not submit a RAP job and does not execute REGENIE.

The generated commands are intended for the REGENIE binary available in the
RAP Swiss Army Knife app or another controlled RAP execution environment.
For the managed RAP REGENIE orchestration app, use the same phenotype,
covariate, sample, and variant-list files through that app's versioned input
interface.
}
