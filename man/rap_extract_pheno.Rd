% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rap_extract.R
\name{rap_extract_pheno}
\alias{rap_extract_pheno}
\title{Extract RAP Phenotype Data Synchronously}
\usage{
rap_extract_pheno(
  field_id = NULL,
  field_names = NULL,
  variables = NULL,
  dataset = NULL,
  output = NULL,
  read = TRUE,
  strip_entity_prefix = FALSE,
  dry_run = FALSE,
  timeout = 300,
  ...
)
}
\arguments{
\item{field_id}{UKB numeric field IDs to extract.}

\item{field_names}{Exact RAP dataset column names to extract.}

\item{variables}{Optional predefined variable names from
\code{get_variable_info()}.}

\item{dataset}{Dataset file name. If NULL, \code{rap_find_dataset()} is used.}

\item{output}{Optional CSV output path in the current RAP session. If NULL,
a temporary file is used.}

\item{read}{Logical. If TRUE, read the CSV into R and return a data.table.
If FALSE, return the output path.}

\item{strip_entity_prefix}{Logical. If TRUE, remove \code{"participant."}
from returned column names.}

\item{dry_run}{Logical. If TRUE, return the extraction plan without running
\code{dx extract_dataset}.}

\item{timeout}{Timeout in seconds for the extraction.}

\item{...}{Additional arguments passed to \code{rap_plan_extract()}.}
}
\value{
A data.table when \code{read = TRUE}; otherwise the output CSV path.
In dry-run mode, returns a \code{rap_extract_plan}.
}
\description{
Uses \code{dx extract_dataset --fields-file} and reads the RAP-generated
result back into R within the active RAP session. This is intended for small
to medium extractions. For large phenotype pulls, use
\code{rap_submit_extract()}.
}
\examples{
\dontrun{
dt <- rap_extract_pheno(field_id = c(31, 53, 21022))
dt <- rap_extract_pheno(variables = c("sex", "age", "bmi"))
}
}
