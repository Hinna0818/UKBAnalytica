% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prs.R
\name{load_ukb_prs}
\alias{load_ukb_prs}
\title{Load Published UK Biobank PRS Fields}
\usage{
load_ukb_prs(
  data = NULL,
  trait = NULL,
  field_id = NULL,
  type = c("standard", "enhanced", "all"),
  id_col = "eid",
  dataset = NULL,
  include_testing = TRUE,
  eligibility = c("set_na", "require", "annotate"),
  standardize = FALSE,
  dry_run = FALSE,
  ...
)
}
\arguments{
\item{data}{Optional participant data.frame. If \code{NULL}, fields are
extracted from RAP.}

\item{trait}{Trait names or abbreviations accepted by
\code{\link[=ukb_prs_catalog]{ukb_prs_catalog()}}.}

\item{field_id}{Optional explicit published PRS field IDs.}

\item{type}{\code{"standard"}, \code{"enhanced"}, or \code{"all"}.}

\item{id_col}{Participant ID column in \code{data}.}

\item{dataset}{Optional RAP dataset passed to \code{\link[=rap_extract_pheno]{rap_extract_pheno()}}.}

\item{include_testing}{Include field 26200 for Enhanced PRS requests.}

\item{eligibility}{Enhanced PRS eligibility handling. \code{"set_na"} keeps all
participants but clears Enhanced scores outside field 26200;
\code{"require"} keeps testing-subgroup participants only; \code{"annotate"} only
appends the indicator.}

\item{standardize}{Add within-sample z-scores with suffix \verb{_z}.}

\item{dry_run}{Return a RAP extraction plan without running it.}

\item{...}{Additional arguments passed to \code{\link[=rap_extract_pheno]{rap_extract_pheno()}}.}
}
\value{
A data.table of participant IDs and named PRS columns, or a
\code{rap_extract_plan} in dry-run mode.
}
\description{
Select already-dispensated PRS fields from a participant table, or extract
them from the current RAP dataset through \code{\link[=rap_extract_pheno]{rap_extract_pheno()}}.
Enhanced requests also include field 26200 by default. No genotype data are
read and no participant-level data are stored in the package.
}
