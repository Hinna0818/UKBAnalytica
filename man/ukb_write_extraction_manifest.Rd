% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rap_manifest.R
\name{ukb_write_extraction_manifest}
\alias{ukb_write_extraction_manifest}
\title{Write a RAP extraction manifest}
\usage{
ukb_write_extraction_manifest(manifest, path, format = c("csv", "rds"))
}
\arguments{
\item{manifest}{A \code{ukb_extraction_manifest} object.}

\item{path}{Output path.}

\item{format}{Output format: \code{"csv"} writes the field table and a sidecar
summary CSV, while \code{"rds"} writes the full manifest object.}
}
\value{
The output path, invisibly.
}
\description{
Write a RAP extraction manifest
}
\examples{
manifest <- ukb_create_extraction_manifest(field_id = c(31, 21022))
tmp <- tempfile(fileext = ".csv")
ukb_write_extraction_manifest(manifest, tmp)
}
