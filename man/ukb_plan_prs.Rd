% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prs.R
\name{ukb_plan_prs}
\alias{ukb_plan_prs}
\title{Plan PRS Calculation with PLINK2}
\usage{
ukb_plan_prs(
  weights,
  bgen = NULL,
  sample = NULL,
  output_prefix,
  keep_file = NULL,
  variant_col = 1L,
  effect_allele_col = 2L,
  weight_col = 3L,
  header = TRUE,
  ref_first = TRUE,
  no_mean_imputation = FALSE,
  frequency_file = NULL,
  error_on_freq_calc = !is.null(frequency_file),
  list_variants = TRUE,
  score_name = "PRS",
  plink2_args = character(),
  genotype = NULL,
  genotype_format = c("auto", "bgen", "pgen", "bed")
)
}
\arguments{
\item{weights}{Normalized output from \code{\link[=prepare_prs_weights]{prepare_prs_weights()}}, a combined
matrix from \code{\link[=combine_prs_weights]{combine_prs_weights()}}, chromosome files from
\code{\link[=split_prs_weights]{split_prs_weights()}}, or a PLINK2 score-file path.}

\item{bgen}{One or more BGEN paths. Retained for backward compatibility;
new code can use \code{genotype} with \code{genotype_format}.}

\item{sample}{One sample path or one path per BGEN. Must be \code{NULL} for
PGEN and BED inputs, whose sample information is part of the file set.}

\item{output_prefix}{Output prefix.}

\item{keep_file}{Optional PLINK two-column sample inclusion file.}

\item{variant_col, effect_allele_col, weight_col}{One-based columns in a raw
score file. Normalized weights always use columns 1, 2, and 3.}

\item{header}{Whether the score file has a header.}

\item{ref_first}{Add the PLINK2 BGEN \code{ref-first} modifier.}

\item{no_mean_imputation}{Add the PLINK2 modifier of the same name.}

\item{frequency_file}{Optional PLINK2 allele-frequency file passed through
\code{--read-freq} to make mean imputation independent of the scoring cohort.}

\item{error_on_freq_calc}{Add \code{--error-on-freq-calc}. By default this is
enabled when \code{frequency_file} is supplied.}

\item{list_variants}{Write the variants actually used by PLINK2.}

\item{score_name}{Name used for a single-score R output. Combined weight
matrices retain their own score names.}

\item{plink2_args}{Additional non-conflicting PLINK2 argument tokens.}

\item{genotype}{One or more BGEN paths, PGEN prefixes/paths, or BED
prefixes/paths.}

\item{genotype_format}{Input format: \code{"auto"}, \code{"bgen"}, \code{"pgen"}, or
\code{"bed"}.}
}
\value{
A \code{ukb_prs_plan} inheriting from \code{ukb_plink2_plan}.
}
\description{
Create one auditable PLINK2 \code{--score} command per genotype input. The plan
requests score sums so chromosome-level results can be added without
averaging artefacts. It does not execute PLINK2 or submit a DNAnexus job.
}
