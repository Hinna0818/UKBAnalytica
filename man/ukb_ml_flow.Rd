% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_workflow.R
\name{ukb_ml_flow}
\alias{ukb_ml_flow}
\title{Run a Complete Single-Model UKB ML Flow}
\usage{
ukb_ml_flow(
  formula = NULL,
  data = NULL,
  split = NULL,
  train_data = NULL,
  test_data = NULL,
  validation_data = NULL,
  id_col = NULL,
  outcome = NULL,
  features = NULL,
  model = "xgboost",
  model_id = "model",
  model_label = NULL,
  outcome_type = c("auto", "binary", "multiclass", "continuous"),
  split_params = list(),
  param_grid = NULL,
  tune = TRUE,
  tune_params = list(),
  best_params = NULL,
  threshold_method = c("none", "fixed", "youden"),
  threshold_params = list(),
  metrics = NULL,
  positive_class = NULL,
  use_validation_in_refit = FALSE,
  compute_shap = FALSE,
  shap_data = NULL,
  shap_params = list(),
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{formula}{Model formula. Required unless both \code{outcome} and
\code{features} are supplied.}

\item{data}{Optional full dataset. Used to create a split when \code{split} is
\code{NULL} and \code{train_data}/\code{test_data} are not supplied.}

\item{split}{Optional \code{ukb_ml_split} object.}

\item{train_data, test_data, validation_data}{Optional pre-split datasets used
when \code{split} is \code{NULL}.}

\item{id_col}{Optional participant ID column for overlap checks and output
predictions.}

\item{outcome}{Optional outcome column. Defaults to the response in
\code{formula} or \code{split$outcome}.}

\item{features}{Optional feature names. Used when \code{formula} is
\code{NULL}.}

\item{model}{Model type passed to \code{\link{ukb_ml_tune}} and
\code{\link{ukb_ml_fit_final}}.}

\item{model_id, model_label}{Optional model identifier and display label.}

\item{outcome_type}{Outcome type.}

\item{split_params}{List passed to \code{\link{ukb_ml_split_data}} when
splitting a full \code{data} object.}

\item{param_grid}{Optional hyperparameter grid.}

\item{tune}{Logical. Run hyperparameter tuning.}

\item{tune_params}{Additional arguments passed to \code{\link{ukb_ml_tune}}.}

\item{best_params}{Optional final model parameters when \code{tune = FALSE}.}

\item{threshold_method}{\code{"none"}, \code{"fixed"}, or \code{"youden"}.}

\item{threshold_params}{Additional arguments passed to
\code{\link{ukb_ml_threshold}}.}

\item{metrics}{Optional metric names passed to \code{\link{ukb_ml_evaluate_test}}.}

\item{positive_class}{Optional positive class label for binary outcomes.}

\item{use_validation_in_refit}{Logical passed to \code{\link{ukb_ml_fit_final}}.}

\item{compute_shap}{Logical. Compute SHAP values for the final model.}

\item{shap_data}{Optional data used for SHAP. Defaults to the frozen test set.}

\item{shap_params}{Additional arguments passed to \code{\link{ukb_shap}}.}

\item{seed}{Optional random seed.}

\item{verbose}{Logical.}
}
\value{
A \code{ukb_ml_flow} object with standardized components:
\code{split}, \code{formula}, \code{features}, \code{tune},
\code{threshold}, \code{final_model}, \code{test_eval},
\code{metrics}, \code{predictions}, \code{roc}, and optional \code{shap}.
}
\description{
High-level single-model interface for common UK Biobank machine-learning
analyses. The function can create or consume a frozen train/test split, tune
model hyperparameters, learn a binary threshold, fit the final model, evaluate
the frozen test set, prepare ROC data, and optionally compute SHAP values.
}
\examples{
\dontrun{
flow <- ukb_ml_flow(
  copd ~ age + bmi + protein_1 + protein_2,
  train_data = train,
  test_data = validation,
  id_col = "eid",
  model = "xgboost",
  threshold_method = "youden"
)
plot(flow, type = "roc")
}

}
