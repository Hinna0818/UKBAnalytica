% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rap_extract.R
\name{rap_submit_extract}
\alias{rap_submit_extract}
\title{Submit a RAP Table-Exporter Phenotype Extraction Job}
\usage{
rap_submit_extract(
  field_id = NULL,
  field_names = NULL,
  variables = NULL,
  dataset = NULL,
  file = NULL,
  instance_type = NULL,
  priority = c("low", "high"),
  dry_run = FALSE,
  manifest = NULL,
  ...
)
}
\arguments{
\item{field_id}{UKB numeric field IDs to extract.}

\item{field_names}{Exact RAP dataset column names to extract.}

\item{variables}{Optional predefined variable names from
\code{get_variable_info()}.}

\item{dataset}{Dataset file name. If NULL, \code{rap_find_dataset()} is used.}

\item{file}{Output file stem on RAP. Defaults to
\code{"ukba_pheno_YYYYMMDD_HHMMSS"}.}

\item{instance_type}{DNAnexus instance type. If NULL, selected from the number
of columns.}

\item{priority}{Job priority: \code{"low"} or \code{"high"}.}

\item{dry_run}{Logical. If TRUE, return the planned fields and command
metadata without uploading or submitting.}

\item{manifest}{Optional manifest CSV path in the current RAP session.}

\item{...}{Additional arguments passed to \code{rap_plan_extract()}.}
}
\value{
A list with class \code{rap_extract_job} containing job metadata.
In dry-run mode, returns a \code{rap_extract_plan}.
}
\description{
Submits an asynchronous RAP \code{table-exporter} job. This is the preferred
interface for large extraction jobs because the work runs on RAP rather than
inside the current R session.
}
\examples{
\dontrun{
job <- rap_submit_extract(field_id = c(31, 53, 21022), file = "baseline_core")
job$job_id
}
}
