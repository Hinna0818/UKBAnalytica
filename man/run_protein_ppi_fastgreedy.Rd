% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Proteomics.R
\name{run_protein_ppi_fastgreedy}
\alias{run_protein_ppi_fastgreedy}
\title{Run fast greedy clustering on a PPI network}
\usage{
run_protein_ppi_fastgreedy(
  ppi,
  n_clusters = 4L,
  largest_component = TRUE,
  cluster_attr = "fast_greedy_cluster",
  prefix = "FG"
)
}
\arguments{
\item{ppi}{An \code{igraph} object, a list returned by \code{get_protein_ppi()}, or a
list containing a \code{graph} element.}

\item{n_clusters}{Integer number of clusters to return. Default is \code{4}.}

\item{largest_component}{Logical. If \code{TRUE}, run clustering on the largest
connected component. If \code{FALSE}, run on the full graph. Default is \code{TRUE}.}

\item{cluster_attr}{Character vertex attribute used to store cluster labels.
Default is \code{"fast_greedy_cluster"}.}

\item{prefix}{Character prefix for cluster labels. Default is \code{"FG"}.}
}
\value{
An \code{igraph} object with a fast greedy cluster vertex attribute.
}
\description{
Cluster a PPI network with igraph's fast greedy community detection. By
default the function uses the largest connected component and cuts the
resulting dendrogram into a user-specified number of clusters. This is useful
when sparse STRING networks contain many small disconnected components but
the case study needs interpretable modules in the main connected network.
}
\examples{
\dontrun{
ppi_fg <- run_protein_ppi_fastgreedy(ppi_graph, n_clusters = 4)
}

}
