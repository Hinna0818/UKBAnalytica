% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/participant_flow.R
\name{ukb_participant_flow}
\alias{ukb_participant_flow}
\title{Build a participant flow table}
\usage{
ukb_participant_flow(
  data,
  steps,
  id_col = NULL,
  outcome_col = NULL,
  event_value = 1,
  start_label = "Initial population"
)
}
\arguments{
\item{data}{A data.frame or data.table.}

\item{steps}{A named list of rules. Each rule can be:
a one-sided formula such as \code{~ !is.na(age)}, a function returning a logical
vector, a logical vector, or a character vector of variable names to retain
complete cases.}

\item{id_col}{Optional participant identifier column. If supplied, duplicate
non-missing IDs are reported as an error.}

\item{outcome_col}{Optional 0/1 outcome column used to count events after
each step.}

\item{event_value}{Value in \code{outcome_col} indicating an event. Defaults to 1.}

\item{start_label}{Label for the first row.}
}
\value{
A data.frame with class \code{ukb_participant_flow}. The kept row index is
stored in \code{attr(result, "kept_index")}.
}
\description{
Apply sequential inclusion or exclusion rules and record the number of
participants retained and removed at each step. Rules can be supplied as
one-sided formulas, functions, logical vectors, or character vectors of
variables requiring complete-case data.
}
\examples{
dat <- data.frame(
  eid = 1:5,
  age = c(50, 60, NA, 55, 70),
  status = c(0, 1, 0, 1, 0)
)
flow <- ukb_participant_flow(
  dat,
  steps = list("Complete age" = "age"),
  id_col = "eid",
  outcome_col = "status"
)
}
