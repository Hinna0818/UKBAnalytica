% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_metadata.R
\name{get_field_info}
\alias{get_field_info}
\title{Get one UK Biobank field's metadata}
\usage{
get_field_info(
  field_id,
  ukb_data_dict = NULL,
  dataset = NULL,
  fields_df = NULL,
  entity = "participant",
  live = FALSE,
  timeout = 30
)
}
\arguments{
\item{field_id}{A single UKB numeric field ID.}

\item{ukb_data_dict}{Optional path to a local \code{Data_Dictionary_Showcase.tsv}
file or equivalent UKB metadata export.}

\item{dataset}{Optional RAP \code{.dataset} file name. Used only when \code{fields_df}
is \code{NULL} and RAP field metadata should be retrieved live.}

\item{fields_df}{Optional data.frame returned by \code{rap_list_fields()}. This is
useful for offline testing or when you already cached the RAP field list.}

\item{entity}{RAP dataset entity. Defaults to \code{"participant"}.}

\item{live}{Logical. If \code{TRUE}, fetch the field page from the public UK
Biobank Showcase website and parse the displayed metadata for this
\code{field_id}.}

\item{timeout}{Timeout in seconds used for the live web request.}
}
\value{
A one-row data.frame when the field is found.
}
\description{
Convenience wrapper around \code{get_field_metadata()} for a single UKB \code{field_id}.
This is the simplest way to ask “what does field 4080 correspond to?” and get
a one-row metadata table back in R.
}
