% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prs_harmonization.R
\name{harmonize_prs_weights}
\alias{harmonize_prs_weights}
\title{Harmonize PRS Weights to a Target Variant Map}
\usage{
harmonize_prs_weights(
  weights,
  variant_map,
  target_id_col = "ID",
  target_chr_col = "CHR",
  target_pos_col = "POS",
  target_allele1_col = "ALT",
  target_allele2_col = "REF",
  target_allele1_freq_col = NULL,
  target_build,
  match_by = c("auto", "id", "position"),
  palindromic = c("drop", "infer_by_af", "keep"),
  af_tolerance = 0.1,
  output = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{weights}{A \code{ukb_prs_weights} object from \code{\link[=prepare_prs_weights]{prepare_prs_weights()}}.}

\item{variant_map}{A data.frame or delimited target variant-map path.}

\item{target_id_col, target_chr_col, target_pos_col}{Target variant columns.}

\item{target_allele1_col, target_allele2_col}{Target allele columns. For a
REF/ALT map, allele 1 would commonly be ALT and allele 2 REF.}

\item{target_allele1_freq_col}{Optional frequency column for target allele 1.}

\item{target_build}{Target genome build, \code{"GRCh37"} or \code{"GRCh38"}.}

\item{match_by}{\code{"auto"} uses ID first and position as a fallback;
\code{"id"} or \code{"position"} restrict matching to one key.}

\item{palindromic}{\code{"drop"}, \code{"infer_by_af"}, or \code{"keep"}. The last option
trusts literal allele orientation and is not recommended without external
justification.}

\item{af_tolerance}{Maximum absolute frequency difference for palindromic
inference. Exactly one orientation must pass this threshold.}

\item{output}{Optional normalized PLINK2 weight-file path.}

\item{overwrite}{Overwrite an existing output.}
}
\value{
A \code{ukb_prs_harmonized} data.frame. Attributes
\code{harmonization_qc} and \code{harmonization_exclusions} record decisions.
}
\description{
Align prepared effect alleles to target genotype alleles before PLINK2
scoring. Matching can use variant ID, chromosome/position, or ID followed by
a position fallback. Direct, swapped, strand, and strand-swapped SNPs are
supported. Indels are retained only when their alleles match directly or in
swapped order; this function does not normalize or left-align indels.

Input and target genome builds must be declared and must match. This
function deliberately does not perform liftOver. Palindromic SNPs are
dropped by default; frequency inference requires both input effect-allele
frequency and target allele-1 frequency.
}
