% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas_format_conversion.R
\name{convert_gwas_datatype}
\alias{convert_gwas_datatype}
\title{Convert Common GWAS Genotype File Formats}
\usage{
convert_gwas_datatype(
  input,
  to,
  output_prefix,
  from = c("auto", "bed", "pgen", "bgen", "vcf", "bcf"),
  sample_file = NULL,
  bgen_ref = c("ref-first", "ref-last", "ref-unknown"),
  vcf_dosage = NULL,
  bgen_version = c("1.2", "1.3", "1.1"),
  bgen_bits = 16L,
  export_ref_first = TRUE,
  vcf_bgz = TRUE,
  vcf_dosage_export = NULL,
  plink2_args = character(),
  execute = FALSE,
  executable = "plink2",
  require_rap = TRUE,
  overwrite = FALSE,
  timeout = 86400
)
}
\arguments{
\item{input}{Input path. For BED and PGEN filesets this may be the fileset
prefix or any primary component path; for BGEN, VCF, and BCF it is the
data-file path.}

\item{to}{Output format: \code{"bed"}, \code{"pgen"}, \code{"bgen"},
\code{"vcf"}, or \code{"bcf"}.}

\item{output_prefix}{Output prefix without a genotype-format extension.}

\item{from}{Input format or \code{"auto"}. Automatic detection uses the
supplied extension and, for a prefix, companion files visible in the
current filesystem.}

\item{sample_file}{Optional BGEN \code{.sample} file or VCF/BCF
\code{.psam} file.}

\item{bgen_ref}{Reference-allele interpretation for BGEN input.}

\item{vcf_dosage}{Optional VCF/BCF dosage field name, for example
\code{"DS"}.}

\item{bgen_version}{BGEN export version.}

\item{bgen_bits}{Integer probability precision for BGEN export. PLINK2
defaults to 16 bits.}

\item{export_ref_first}{Put REF first when exporting BGEN.}

\item{vcf_bgz}{Block-gzip VCF output.}

\item{vcf_dosage_export}{Optional VCF/BCF dosage export mode: \code{"GP"},
\code{"DS"}, \code{"DS-force"}, \code{"DS-only"}, \code{"HDS"}, or
\code{"HDS-force"}.}

\item{plink2_args}{Additional PLINK2 argument tokens, such as
\code{c("--maf", "0.01", "--geno", "0.05", "--threads", "16")}.
Input/output flags managed by this function are rejected. Use
\code{\link{ukb_plan_plink2}} for a fully custom command.}

\item{execute}{Execute the conversion. Defaults to \code{FALSE}.}

\item{executable}{PLINK2 executable path or command name.}

\item{require_rap}{Require a RAP-like environment before execution.}

\item{overwrite}{Permit PLINK2 to replace an existing expected output.}

\item{timeout}{Command timeout in seconds.}
}
\value{
With \code{execute = FALSE}, a
\code{ukb_gwas_conversion_plan}; otherwise a \code{ukb_plink2_run}.
}
\description{
Build or execute a PLINK2 command that converts between common GWAS
genotype formats. Supported high-level formats are PLINK 1 binary
(\code{bed/bim/fam}), PLINK 2 binary (\code{pgen/pvar/psam}), BGEN,
VCF, and BCF.

The function is a dry run by default. It does not install PLINK2, submit a
DNAnexus job, or copy participant data into the package. With
\code{execute = TRUE}, it calls an existing PLINK2 binary through
\code{\link{ukb_run_plink2}}.
}
