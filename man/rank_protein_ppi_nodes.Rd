% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Proteomics.R
\name{rank_protein_ppi_nodes}
\alias{rank_protein_ppi_nodes}
\title{Rank nodes in a PPI network by integrated centrality}
\usage{
rank_protein_ppi_nodes(
  ppi,
  metrics = c("degree", "betweenness", "closeness", "eccentricity", "radiality",
    "Stress", "MCC", "MNC", "DMNC", "BN", "EPC"),
  weights = NULL,
  use_weight = TRUE,
  na_rm = TRUE
)
}
\arguments{
\item{ppi}{An \code{igraph} object, a list returned by \code{get_protein_ppi()}, or a
list containing a \code{graph} element.}

\item{metrics}{Character vector of node metrics used for integrated ranking.}

\item{weights}{Optional numeric weights for \code{metrics}.}

\item{use_weight}{Logical. Whether to prefer weighted betweenness and
closeness metrics. Default is \code{TRUE}.}

\item{na_rm}{Logical. Whether to ignore missing values during normalization.
Default is \code{TRUE}.}
}
\value{
A list with components \code{graph} and \code{table}.
}
\description{
A thin wrapper around \code{TCMDATA::rank_ppi_nodes()}.
}
\examples{
\dontrun{
rank_res <- rank_protein_ppi_nodes(ppi_metrics)
head(rank_res$table)
}

}
