% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_skeleton.R
\name{ukb_time_skeleton}
\alias{ukb_time_skeleton}
\title{Build a UK Biobank follow-up time skeleton}
\usage{
ukb_time_skeleton(
  data,
  id_col = "eid",
  baseline_col = "p53_i0",
  birth_year_col = "p34",
  birth_month_col = "p52",
  age_col = "p21022",
  death_date_cols = "^(participant\\\\.)?p40000_i[0-9]+$",
  lost_to_followup_col = "p191",
  admin_censor_date = as.Date("2023-10-31"),
  keep_source_dates = TRUE
)
}
\arguments{
\item{data}{A data.frame or data.table containing UK Biobank columns.}

\item{id_col}{Participant identifier column. Default \code{"eid"}.}

\item{baseline_col}{Baseline assessment date column. Default \code{"p53_i0"}.}

\item{birth_year_col}{Year-of-birth column. Default \code{"p34"}.}

\item{birth_month_col}{Month-of-birth column. Default \code{"p52"}.}

\item{age_col}{Age-at-baseline column. Default \code{"p21022"}. If missing,
age is approximated from baseline date and birth year/month when available.}

\item{death_date_cols}{Death date columns or a regular expression used to
identify them. Default \code{"^(participant\\.)?p40000_i[0-9]+$"}.}

\item{lost_to_followup_col}{Optional date lost to follow-up column. Default
\code{"p191"}.}

\item{admin_censor_date}{Administrative censoring date.}

\item{keep_source_dates}{Logical. If \code{FALSE}, source dates used to define
censoring are removed from the output.}
}
\value{
A data.table with one row per participant and standardized follow-up
time fields.
}
\description{
Creates a reusable participant-level time skeleton for prospective UK
Biobank analyses. The function standardizes baseline date, approximate birth
date, age at baseline, death date, loss-to-follow-up date, administrative
censoring date, follow-up end date, and follow-up time. It does not define
disease outcomes; instead, it provides a common time basis that can be reused
by endpoint-specific functions such as \code{\link{build_survival_dataset}}.
}
\examples{
demo <- data.frame(
  eid = 1:3,
  p53_i0 = as.Date(c("2010-01-01", "2011-01-01", "2012-01-01")),
  p21022 = c(50, 60, 70),
  p40000_i0 = as.Date(c(NA, "2015-01-01", NA))
)

ukb_time_skeleton(demo, admin_censor_date = as.Date("2020-12-31"))

}
