% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas_phewas_tools.R
\name{ukb_run_regenie}
\alias{ukb_run_regenie}
\title{Run a Planned REGENIE Analysis}
\usage{
ukb_run_regenie(
  plan,
  execute = FALSE,
  steps = c("step1", "step2"),
  executable = "regenie",
  require_rap = TRUE,
  timeout = 86400
)
}
\arguments{
\item{plan}{A plan inheriting from \code{ukb_regenie_plan}.}

\item{execute}{Logical. Defaults to \code{FALSE}; when false, the plan is
returned without running anything.}

\item{steps}{Commands to run. For a structured plan, use
\code{"step1"}, \code{"step2"}, or both. For a raw command plan, use its
command label. \code{NULL} runs every command in the plan.}

\item{executable}{REGENIE executable path or command name.}

\item{require_rap}{Require a RAP-like environment before execution.}

\item{timeout}{Per-command timeout in seconds.}
}
\value{
The unchanged plan when \code{execute = FALSE}; otherwise a list of
command statuses and captured logs.
}
\description{
Execute direct REGENIE commands from \code{\link{ukb_plan_regenie}} or
\code{\link{ukb_plan_regenie_command}}.
Execution is opt-in and requires a REGENIE binary. On RAP this is intended
for an existing Swiss Army Knife or controlled custom execution
environment; it does not itself submit a DNAnexus job.
}
